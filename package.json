{
  "name": "sasstree",
  "version": "0.0.8",
  "description": "A Sass parser built in JavaScript.",
  "main": "lib/Parser.js",
  "bin": {
    "sasstree": "bin/sasstree"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src --out-dir lib",
    "web": "watchify src/Browser.js -o bin/browser.js -t babelify --debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Furnes <david@dfurnes.com>",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.8.1",
    "lodash": "^3.3.1"
  },
  "devDependencies": {
    "babel": "^4.6.1",
    "babelify": "^6.1.3",
    "browserify": "^9.0.3",
    "watchify": "^3.3.1"
  }
}
